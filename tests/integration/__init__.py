"""Integration tests (opt-in): see ``test_live_providers.py``.

Nothing here runs in the default offline suite — every test requires
``AGENT_FACTORY_LIVE_TESTS=1`` plus a reachable live provider.
"""
